import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'directivasS3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
